export const cellHeight = 64;

export const estimateHeight = 24;
export const actualHeight = 6;

export const estimateTop = (cellHeight - estimateHeight) / 2;
export const actualTop = (cellHeight - actualHeight) / 2;
